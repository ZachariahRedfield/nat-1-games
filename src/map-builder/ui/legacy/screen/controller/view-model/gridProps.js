export function createGridProps(state) {
  return {
    layers: state.layers,
    maps: state.maps,
    objects: state.objects,
    assets: state.assets,
    engine: state.engine,
    selectedAsset: state.selectedAsset,
    gridSettings: state.gridSettings,
    stampSettings: state.assetStamp,
    setGridSettings: state.setGridSettings,
    brushSize: state.brushSize,
    canvasOpacity: state.canvasOpacity,
    canvasColor: state.canvasColor,
    canvasSpacing: state.canvasSpacing,
    canvasBlendMode: state.canvasBlendMode,
    canvasSmoothing: state.canvasSmoothing,
    naturalSettings: state.naturalSettings,
    isErasing: state.isErasing,
    interactionMode: state.interactionMode,
    tileSize: state.tileSize,
    scrollRef: state.scrollRef,
    contentRef: state.gridContentRef,
    canvasRefs: state.canvasRefs,
    currentLayer: state.currentLayer,
    layerVisibility: state.layerVisibility,
    tokensVisible: state.tokensVisible,
    tokenHUDVisible: state.tokenHUDVisible,
    tokenHUDShowInitiative: state.tokenHUDShowInitiative,
    assetGroup: state.assetGroup,
    showGridLines: state.showGridLines,
    zoomToolActive: state.zoomToolActive,
    panToolActive: state.panToolActive,
    onZoomToolRect: state.handleZoomToRect,
    onBeginTileStroke: state.onBeginTileStroke,
    onBeginCanvasStroke: state.onBeginCanvasStroke,
    onBeginObjectStroke: state.onBeginObjectStroke,
    onBeginTokenStroke: state.onBeginTokenStroke,
    placeTiles: state.placeTiles,
    addObject: state.addObject,
    eraseObjectAt: state.eraseObjectAt,
    moveObject: state.moveObject,
    removeObjectById: state.removeObjectById,
    updateObjectById: state.updateObjectById,
    onSelectionChange: state.handleSelectionChange,
    selectedObjsList: state.selectedObjsList,
    selectedTokensList: state.selectedTokensList,
    tokens: state.tokens,
    addToken: state.addToken,
    moveToken: state.moveToken,
    removeTokenById: state.removeTokenById,
    updateTokenById: state.updateTokenById,
    onTokenSelectionChange: state.handleTokenSelectionChange,
    allowInactiveSelection: true,
  };
}
